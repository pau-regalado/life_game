#include "../include/SortMethod.h"
/*
//Recibe un puntero al vector a ordenar y su tamaño y lo ordena.
template <class Key>
void SortMethod<Key>::mostrar_v(std::vector<Key>* v){
  std::cout << "< ";
  for (int i = 0; i < v->size(); ++i)
    std::cout << v->at(i) << " ";
  std::cout << ">" << std::endl;
}

template <class Key>
void SortMethod<Key>::swap(std::vector<Key>* vector, int x, int y){
  Key aux = vector->at(x);
  vector->at(x) = vector->at(y);
  vector->at(y) = aux; 
}
*/